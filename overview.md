# 交付概览 — 工艺单上传 BUG 修复（2026-09-10）

## TL;DR

你报告的「工艺项被多读 + 制衣厂名字进了工艺名称框、制衣厂框反而留空」**不是代码映射错误**，
而是**部署链路断裂**：本机一直运行 7/28 的旧版 Skill，9/9 的重构与修复从未打包分发。
现已完成：SKILL.md 加固（禁读图/禁改表/只报不改/改前必确认）、解析层 7 项健壮化、
分发包重建、**本机安装已覆盖为新版**。

## 根因（代码级证据闭环）

| 层 | 证据 | 结论 |
|---|---|---|
| 部署 | 安装目录 `SKILL.md` 608 行(7/28)、`src/` 为旧 7 模块（含 `field_mapper.py`） | 线上跑的是旧版，仓库修复从未生效 |
| 旧代码 | `old field_mapper.py:118` `"factoryName": "",  # 留空` | 制衣厂必然为空 ✅ 吻合症状 |
| 旧代码 | `old field_mapper.py:99-111` 按 `；，,` 拆分 `process_notes` | 实跑 **1 行备注 → 21 条工艺项** ✅ 吻合症状 |
| 旧 SKILL | 「场景A 对话填表 / 拖图→Agent 生成 Excel」+「未确认前只允许修改数据」 | Agent 据此读图自行补数据、改表 |

**代码映射本身无误**：基础信息 9 处、SKU 区 17 处、辅料区 9 处坐标与 V7 模板实测**逐一一致**。

## 修复清单

| 文件 | 变更 |
|---|---|
| `SKILL.md` | 新增「铁律」章节（禁止读图 / 禁止擅自改表，唯一白名单=B6 回填 / 只报不改 / 改前必确认）；修正工艺区术语为「工艺名称(B列)/制衣厂(C列)/单价(D列)」；去掉硬编码 `F:\` 路径 |
| `src/excel_parser.py` | 间隔行越界修复；无效行/缺分区标题/缺辅料名称**一律告警上报**（不静默丢弃）；表头判定改结构化；工艺区上界与 SKU/辅料表头改**动态全表扫描**；幅宽兜底修正；剔除空 SKU 幻影组 |
| `src/submit_excel.py` | 输出 `[WARN]` 无效行告警 |
| `scripts/build_skill_zip.py` | **新增**：固化分发包重建（显式条目 + 8 项自校验） |
| `craft-sheet-automation.zip` | 重建：13 条目，zip 内文件与仓库 md5 一致 |
| `.gitignore` | 忽略 `tests/`（含真实客户/供应商/手机号）与 SKILL 说明文档（仅存 IMA） |

## 验证结果

- **本地自测**：间隔行写入不再静默、表头变体「工艺名称（下拉）」不再多出条目、V5/V6/V7 空模板零脏数据、7 项告警文案用例全对
- **架构师全量自查**：无列映射 BUG；发现并已修复 6 项同类健壮性缺陷
- **QA 独立回归**：**PASS**（含旧版根因复现、zip↔仓库 md5、对抗性插行/改表头测试）
- **部署后核验**：`SKILL.md` 338 行含铁律、9 模块、旧模块零残留、无硬编码路径

## 待办

1. **推送 GitHub**：网络 443 被阻断（`Connection was reset`），本地积压 8 个提交。恢复后执行
   `git push origin HEAD`（备份：本地提交已安全落库，不会丢）。
2. **可选**：将 `SA设计版单自动化上传_SKILL说明_v2.1.md` 上传 IMA 知识库（`SYNERGY美学与技术实验室`，KB `7462422730275604`），与 v1.0/v2.0 并存。
3. **团队更新**：推送到 GitHub 后，通知各成员拉取并覆盖 `~/.workbuddy/skills/craft-sheet-automation/`。

## 回滚方式

旧版完整备份在 `~/.workbuddy/skills/_backup_craft-sheet-automation_2026-09-10/`，
如新版异常，用该目录覆盖回 `~/.workbuddy/skills/craft-sheet-automation/` 即可。
