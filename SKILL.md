---
name: craft-sheet-automation
description: SA设计版单自动化上传 — 设计师填写Excel+准备图片，一键上传至打版管理系统。
modelIsThinking: false
agent_created: true
---

# TB-SA设计版单自动化上传 Skill

> **项目根目录**: `F:\WORKBUDDY WS\工艺单数据表单自动化`
> 所有 Python 脚本位于 `src/`，所有相对路径以此项目根目录为基准。

## 触发词

设计师说：上传工艺单 / 填工艺单 / 帮我上传 / 提交工艺单

也可以是拖入 JPG 图片 + 任意一句"帮我处理这个工艺单"

## 铁律（最高优先级，任何流程不得违反）

1. **禁止读图**：严禁以任何方式读取、打开、查看、识别随附图片的内容（含 OCR 与视觉理解）。图片仅作附件上传，Agent 不得从图片中提取、推断、补全任何数据，更不得据图新增工艺行、辅料或任何字段。
2. **禁止擅自改表**：对设计师的 Excel 一律**只读**。唯一白名单——提交成功后、且 B6 款号原本为空时，才允许把系统自动生成的款号写回 B6。除此以外，禁止任何单元格写入（含新增/删除行、改值、跨列搬移字段）。
3. **只报不改**：审查产出只能是「缺失/疑问清单」，一律回问设计师，由设计师补充；Agent 不得代填、不得据图补全、不得凭常识推断。
4. **改前必确认**：确需修改任何单元格时，必须先列出「单元格 → 现值 → 拟改值」，取得设计师**逐项**同意后方可执行。

---

## 上传方式（唯一模式）

> ⚠️ **本 Skill 只有一种上传方式**：设计师填好 **Excel 表格 + 图片文件夹**，Agent 审查后一键上传。
> **不存在**「对话填表」或「A/B 方案」等其他上传方式。收到上传请求后，直接走下面的标准流程，**不要询问设计师用哪种方式**。

### 标准流程

```
设计师提供「Excel + 图片文件夹」
       ↓
Agent 审查 Excel（逐区检查）
       ↓
列出缺失/疑问 → 设计师补充
（Agent 只负责列出，不负责填写）
       ↓
设计师确认
       ↓
上传图片 + API 提交
       ↓
显示结果
```

### 文件准备要求

设计师需要提供 **1 个文件夹**，内含：

| 文件 | 说明 |
|------|------|
| Excel 表格 | 用 `config/成衣工艺单_设计师填表模板_v7.xlsx` 填写 |
| 图片 | 按规则命名，放在同一文件夹 |

### 图片命名规则

| 命名 | 对应位置 |
|------|---------|
| `A.png` / `A.jpg` | 主图（基础信息） |
| `B.png` / `B.jpg` | SKC 详情图 |
| `C1.png` / `C1.jpg` | 工艺详情第 1 张 |
| `C2.png` / `C2.jpg` | 工艺详情第 2 张 |
| `C3.png` ... | 工艺详情第 3 张起（支持任意张数） |

> 图片可用 JPG 或 PNG 格式，文件名只保留字母和数字（如 `A.png`），无需加款号前缀。提交成功后系统自动重命名为 `{款号}_A.png`。

### 文件夹结构示例

```
📁 我的设计单/
├── 📄 表单.xlsx          ← 用 v7 模板填好
├── 🖼️ A.png              ← 主图
├── 🖼️ B.png              ← SKC 详情
├── 🖼️ C1.png             ← 工艺详情1
├── 🖼️ C2.png             ← 工艺详情2
└── 🖼️ C3.png             ← 工艺详情3
```

提交成功后，文件夹、表格全部自动重命名为 `{款号}_{姓名}` 格式，图片重命名为 `{款号}_{位置}.png`，方便按姓名检索。

> ⚠️ **严禁读图**：不得以任何方式读取/打开/查看/识别（含 OCR 及视觉理解）随附图片的内容。图片仅作附件上传，不得从图片中提取、推断、补全任何数据，不得据图新增工艺行、辅料或任何字段。

---

## 交互规范

### 选择类问题：使用 WorkBuddy 可交互问卷

当下拉选项 ≤ 4 个时，使用 `AskUserQuestion` 工具以表格+按钮形式呈现，设计师直接点击选择，不需要打字。

示例：
```
需求类型选哪个？
  [OEM]  [ODM]  [OBM]
```

当选项超过 4 个时，先以紧凑表格列出全部选项（附编号），再让设计师输入编号或名称。

### 全部列出原则

凡是涉及选项（烫唛、颜色、工艺、辅料等），**必须全部列出**所有可选项，设计师不需要跳转到前端找。

---

## 审查 Excel

Agent 读取设计师提供的 Excel 文件，逐区检查：

| 检查区 | 必填项 | 常见缺失 |
|--------|--------|---------|
| 基础信息 | 款号、需求类型、客户、衣服类型、款式、设计师 | 版型、烫唛、完成时间 |
| 面料区 | 布类(A/B/C)、编号、色号、颜色名、成分、克重、幅宽、供应商 | 采购人(cgBy)、螺纹标记 |
| 尺码 | 尺码、件数 | — |
| 工艺区 | 工艺名称（B列） | 制衣厂（C列）、单价（D列） |
| 辅料区 | 名称、规格、颜色、用量、单位、采购单价、供应商、联系方式、采购数量、采购人、到货时间 | 采购人 |

> 审查 = 只读核对并报告缺失，不新增、不补全、不跨列搬移。

### 列缺失清单

以表格形式列出所有缺失/待确认项，交由设计师补充。Agent 不得自行推断、不得据图片补全、不得修改任何单元格。不逐条重问已有数据。

### 补充确认

设计师补充缺失项后，展示最终审核表。设计师明确回复"确认"后提交。

### 提交门槛（CRITICAL）

> ⚠️ **设计师必须明确回复"确认"后，才能执行提交。**
> 未收到"确认"之前，禁止对 Excel 做任何写入。如需修改，必须先列出「单元格 → 现值 → 拟改值」，取得设计师逐项同意后才可改，且只改被明确同意的单元格。

---

## 首次使用：收集使用者身份

本 Skill 安装后首次加载时，必须向使用者询问以下信息：

> 欢迎使用工艺单上传 Skill！首次使用需要配置你的信息：
>
> **A. 身份信息**（用于自动填入设计师和采购人）
> 1. 你的真实姓名（如 张三）→ 面料/辅料的采购人(cgBy)默认填你
> 2. 你在打板系统里的设计师名字（如 张三）
> 3. 你在系统里的设计师编号（如 75）
>
> **B. 嘉谦系统登录凭据**（用于 API 自动上传）
> 4. 嘉谦系统的登录账号
> 5. 嘉谦系统的登录密码

收集后写入 `src/.identity.json`：
```json
{"realname": "张三", "designer_id": "75", "username": "账号", "password": "密码"}
```

## 采购人(cgBy)规则

采购人和指派设计师**默认均为使用者姓名**（`realname`），除非：

| 情况 | 行为 |
|------|------|
| Excel 表格中采购人列为空 | → 填使用者姓名 |
| Excel 表格中采购人列有值（如"圆圆"） | → **以表格中的值为准**，不覆盖 |
| 辅料的采购人列为空 | → 填使用者姓名 |
| 辅料的采购人列有值 | → **以表格中的值为准** |

> 换言之：使用者姓名是默认值，表格里有明确写明的采购人具有更高优先级。

> 以上默认值/映射仅用于生成 API payload，**禁止据此改写 Excel 单元格**。

---

## API 数据结构注意事项（铁律）

从多轮调试中总结的关键规则，提交时必须遵守：

### fabricList 必填字段

| 字段 | 说明 | 示例 |
|------|------|------|
| `productNo` | **面料编号**（必须！否则前端无法匹配） | `"JQ5665"` |
| `clothType` | **布类**：第一块A，第二块B，第三块C... | `"A"` / `"B"` |
| `color` | 色号 | `"5665-2(CG)"` |
| `colorName` | 颜色名 | `"活性黑"` |
| `cgBy` | **采购人**（默认同设计师） | `"设计者姓名"` |
| `fabricId` | 面料库ID | `167` |
| `supNo` / `supName` | 供应商编号 / 名称 | `"G0002"` / `"嘉谦纺织"` |
| `component` | 成分 | `"85%棉8.5%聚酯纤维6.5%氨纶"` |
| `buffon` | 幅宽 | `"183CM"` |
| `grem` | 克重 | `"340G（回后）"` |
| `thread` | 螺纹标记 | `"0"` 否 / `"1"` 是 |
| `sort` | 排序 | `1, 2, 3...` |
| `type` | 面料类型（[NEW]） | `""` |
| `colorTypeVOS` | 颜色列表（[NEW]） | `[{"color":"5665-2","colorName":"活性黑"}]` |

### SKU 字段

- `sku` / `skuName`：保持独立标识（如 `"SKU1"`），**禁止填入面料编号、颜色、尺码**
- SKU 行颜色：需单独询问设计师（不自动从面料取）

### works 工艺字段（2026-07 更新）

| 字段 | 说明 | 示例 |
|------|------|------|
| `cate` | 工艺类型：`"0"`=工艺制作，`"1"`=车缝工艺，`"2"`=成衣工艺 | `"0"` |
| `status` | 固定 `"0"` | `"0"` |
| `name` | 工艺名称 | `"三针五线大哈苏"` |
| `sort` | 排序号 | `0, 1, 2...` |
| `factoryName` | **[NEW]** 制衣厂 | `"制衣厂"` |
| `unitPrice` | **[NEW]** 单价 | `2.13` |

### 新增顶层字段（2026-07 更新）

| 字段 | 说明 |
|------|------|
| `customerLogo` | 客户 Logo，默认留空 |
| `sewing` | 已废弃，车缝工艺统一在 `works[].cate='1'` |
| `patternBy` / `cutBy` / `sewingBy` | 纸样/裁剪/车缝负责人 |
| `patternTime` / `cutTime` / `sewingTime` | 各阶段完成时间 |
| `status` | 固定 `0` |

### 辅料 auxiliaries 更新

| 新增字段 | 说明 |
|------|------|
| `supListVOS` | **[NEW]** 供应商列表对象，含 `supNo`/`supName`/`price` |

### 其他

- `layout`（版型）：必填
- `customerCode`：从客户API模糊匹配获取；无匹配时留空
- `cgBy` 规则：面料/辅料的采购人**默认同使用者姓名**。**但 Excel 表格中采购人列如有明确值（非空、非"请选择"），以表格值为准，不覆盖。**

> 以上默认值/映射仅用于生成 API payload，**禁止据此改写 Excel 单元格**。

### 款号为空时的处理

若 Excel 中款号（B6）未填：

1. API 提交时 `itemNum` 留空 —— 系统自动生成款号
2. 提交成功后，查询已创建的设计单，获取系统自动生成的款号
3. 用该款号重命名：
   - Excel 文件：`原文件名` → `{系统款号}_{姓名}.xlsx`
   - 图片文件：`{系统款号}_A.png`, `{系统款号}_B.png` ...
4. 在对话中告知设计师："款号未填，系统已自动生成款号 XXX，文件已同步重命名"

> 唯一允许写回 Excel 的位置是 B6 单元格，且仅在提交成功后、原值为空时执行；其他单元格一律不动。

---

## 环境切换（铁律）

> ⚠️ **默认模式：正式系统**。仅在用户明确说「进入沙箱模式」时才切换到沙箱。
> **严禁在正式系统中做测试。**

| 模式 | 触发词 | 地址 | .mode 内容 |
|------|--------|------|-----------|
| 正式系统（默认） | — | `https://mc.fsjqfz.xyz` | `production` |
| 沙箱模式 | "进入沙箱模式" | `https://mctwo.fsjqfz.xyz` | `sandbox` |
| 退出沙箱 | "退出沙箱模式" | 切回正式系统 | `production` |

> ★ 两个地址的唯一区别：`mc` 后面有无 `two`。正式系统 `mc.fsjqfz.xyz`，沙箱 `mctwo.fsjqfz.xyz`。
> 切换方式：修改 `src/.mode` 文件内容（`production`/`sandbox`），`config.py` 自动读取并决定 API 地址。
> ⚠️ `.mode` 与 `.identity.json` 均为本地文件，已加入 `.gitignore`，不会提交到仓库。

## 沙箱环境（测试用）

| 项目 | 值 |
|------|-----|
| 地址 | https://mctwo.fsjqfz.xyz |
| API基路径 | /makeCloth |
| 用户名 | （首次运行自动收集） |
| 密码 | （首次运行自动收集） |
| 登录接口 | `POST /makeCloth/colorimeter/index/login` |
| 图片上传 | `POST /makeCloth/business/upload/img` |
| 创建设计单 | `POST /makeCloth/business/design/insertDesign` |
| 编辑设计单 | `POST /makeCloth/business/design/updateDesign` |
| 设计单查询 | `POST /makeCloth/business/design/selectList` |
| 设计师查询 | `GET /makeCloth/dept/getUserList/name?deptName=设计部` |
| 客户查询 | `GET /makeCloth/business/customer/public` |
| 唛头查询 | `POST /makeCloth/business/markLabel/select` |

## Excel 模板映射（实测坐标，与 excel_parser.py 一致）

> 模板：`config/成衣工艺单_设计师填表模板_v7.xlsx`（代码读取 Sheet1 活动工作表）。
> SKU 区与辅料区为**动态表头扫描**（按「SKU编号」/「辅料名称」定位），坐标因模板版式可能位移，勿硬编码固定行。

### 基础信息区（Sheet1 顶部）

| Excel 单元格 | API 字段 | 说明 |
|-------------|---------|------|
| B6 | `itemNum` | 款号（留空 → 系统自动生成，成功后回填） |
| F6 | `needType` | 需求类型 (OEM/ODM/OBM) |
| B7 | `clothType` | 衣服类型 (T/W/J/K...) |
| F7 | `style` | 款式 |
| B8 | `customerName` | 客户名称（API 模糊匹配补 `customerCode`） |
| B9 | `num` | 打版件数（缺省按 1） |
| F9 | `layout` | 版型 |
| B10 | `markName` | 烫唛 |
| F10 | `finishTime` | 预计完成时间（支持中文日期：下周三/月底/明天） |

### 工艺区（works[]，约 12-35 行，按 A 列分区标题识别）

> 严格一一对应：B=工艺名称→works[].name，C=制衣厂→works[].factoryName，D=单价→unitPrice。严禁把制衣厂写入工艺名称列、严禁 B/C 合并、严禁新增或复制工艺行。

| Excel 列 | API 字段 | 说明 |
|---------|---------|------|
| A 列标题 | 分区 | `工艺制作`/`车缝工艺`/`成衣工艺` → `cate` 0/1/2 |
| B | `works[].name` | 工艺名称 |
| C | `works[].factoryName` | 制衣厂 |
| D | `works[].unitPrice` | 单价 |

### SKU/面料区（designColors[].fabricList，动态扫「SKU编号」表头，V7 模板实测约第 34 行）

| Excel 表头列 | API 字段 | 说明 |
|-------------|---------|------|
| SKU编号(A) | `designColors[].sku/skuName` | 取 SKU1/SKU2...（禁止颜色_尺码） |
| 颜色 / 尺码 | `color` / `size` | 存 designColors 层 |
| 件数 | `num` | 每 SKU 打版件数 |
| 布类 / 面料编号 | `fabricList[].clothType` / `productNo` | 必填 |
| 色号 / 颜色名 | `fabricList[].color` / `colorName` | 必填（缺失拦截） |
| 是否罗纹 / 幅宽CM / 克重 | `isRib` / `buffon` / `grem` | 不在 DB 时手工补充 |
| 供应商 / 成分 / 品名 | `supName` / `component` / `productName` | JQ 开头自动拉 DB |
| 采购米长 / 采购金额 / 采购人 / 要求到货时间 | `meterLen` / `meterPrice` / `cgBy` / `arriveTime` | 采购人空→默认使用者 |

### 辅料区（auxiliaries[]，动态扫「辅料名称」表头，V7 模板实测约第 45 行）

| Excel 表头列 | API 字段 |
|-------------|---------|
| 辅料名称 / 规格型号 / 颜色 | `name` / `type` / `color` |
| 用量 / 单位 | `usageAct` / `unit`（单位空默认"个"） |
| 采购单价 / 供应商 / 联系方式 | `price` / `supName` / `phone` |
| 采购人 | `cgBy`（空→默认使用者） |

> 以上默认值/映射仅用于生成 API payload，**禁止据此改写 Excel 单元格**。
