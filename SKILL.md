---
name: TB-SA设计版单自动化上传
description: TB-SA设计版单自动化上传 — 设计师填写Excel+准备图片，一键上传至打版管理系统。
modelIsThinking: false
agent_created: true
---

# TB-SA设计版单自动化上传 Skill

> **项目根目录**: `F:\WORKBUDDY WS\工艺单数据表单自动化`
> 所有 Python 脚本位于 `src/`，所有相对路径以此项目根目录为基准。

## 触发词

设计师说：上传工艺单 / 填工艺单 / 帮我上传 / 提交工艺单

也可以是拖入 JPG 图片 + 任意一句"帮我处理这个工艺单"

## 交互规范

### 选择类问题：使用 WorkBuddy 可交互问卷

当下拉选项 ≤ 4 个时，使用 `AskUserQuestion` 工具以表格+按钮形式呈现，设计师直接点击选择，不需要打字。

示例：
```
需求类型选哪个？
  [OEM]  [ODM]  [OBM]
```

当选项超过 4 个时，先以紧凑表格列出全部选项（附编号），再让设计师输入编号或名称。

### 全部列出原则

凡是涉及选项（烫唛、颜色、工艺、辅料等），**必须全部列出**所有可选项，设计师不需要跳转到前端找。

## 工作流路由（双场景）

设计师触发上传后，第一件事是**判断场景**：

```
设计师拖入图片 + "帮我上传"
       ↓
  Agent 询问: "Excel 表格已经填好了吗？"
       ↓
  ┌─ 还没填 → ① 建议用户填表（指向V6模板）
  │           ② 复制 V6 模板到用户桌面
  │           ③ 展示 A.1 填表指引 → 对话逐区填表
  │
  └─ 已经填好了 → 场景 B：直接审查 Excel（设计师已填好）
```

> ⚠️ **「还没填」时必须先复制 V6 模板到桌面**，再展示填表指引。
> 复制命令：`cp "config/成衣工艺单_设计师填表模板_v6.xlsx" "$HOME/Desktop/成衣工艺单_填表模板.xlsx"`

### 场景细分

| 设计师给了什么 | 场景 |
|---------------|------|
| 只有图片（JPG），Excel未填 | **场景 A**：对话填表 → Agent生成Excel → 上传图片+提交 |
| 图片 + Excel 都给了 | **场景 B**：Agent审查Excel → 补缺 → 上传图片+提交 |
| 只有图片，但设计师说Excel已填好 | **场景 B**：Agent索要Excel文件 → 审查 → 上传图片+提交 |

> ⚠️ **图片仅作为附件上传**，不用于OCR识别或数据提取。所有数据来自对话或Excel。

---

## 场景 A：对话填表（Excel 未填）

```
对话逐区收集（设计师口述）→ Agent生成Excel → 审核预览 → 确认 → 上传图片+API提交 → 显示结果
```

> ⚠️ **图片不参与数据识别**。图片仅作为附件在 A.4 提交环节上传到API，所有工艺单数据均来自对话中设计师的口述输入，不做OCR。

### A.1 填表指引 — 以不可交互表格展示全部需填字段

> ⚠️ **当前策略**：由于 WorkBuddy `show_widget` 中 `sendPrompt()` 存在兼容性问题，表单数据无法回传。暂不使用交互式表单，改用「展示需填字段表 → 设计师口述逐区填报」模式。`assets/form.html` 保留不动，待兼容性修复后再恢复。

**流程**：Agent 在对话中以 Markdown 表格展示以下四区的全部字段，设计师根据表格逐区口述填写，不限顺序。

#### 第一区：基础信息

| 字段 | 必填 | 选项/格式 |
|------|------|-----------|
| 款号 | 否 | 自由文本，如 293B09W-S；留空则系统自动生成 |
| 需求类型 | 是 | OEM / ODM / OBM |
| 客户名称 | 是 | 自由文本，后续做模糊匹配（见1.1客户名称匹配流程） |
| 衣服类型 | 是 | T/W/J/K/B/F/C/P/H/S/Q |
| 款式 | 是 | 短袖T恤/短裤/长裤/长袖T恤/半拉链外套/连帽开衫外套/圆领卫衣/连帽卫衣/短裙/连衣裙/短袖衬衫/长袖衬衫/拉链开衫外套/背心/polo衫/包包/套头卫衣/开衫卫衣/半拉链卫衣 |
| 打版件数 | 否 | 默认 1 |
| 版型 | 否 | 自由文本 |
| 烫唛 | 否 | 无领唛/SYNERGY烫唛/SYNERGY白织唛/SYNERGY黑织唛/四圈无商标烫唛/液氨专用烫唛/漫威龙烫唛/漫威龙织唛/CONP后领标/CIS上装烫麦/CIS下装烫麦/CCC烫唛Unisex/CCC烫唛Women's/CCC烫唛Men's/CCC帆船唛all/CCC帆船唛girls/CCC帆船唛BOYS/失物招领烫唛/失物招领白织唛 |
| 预计完成时间 | 否 | YYYY-MM-DD |
| 指派设计师 | 否 | 默认当前使用者 |

#### 第二区：工艺制作

| 字段 | 必填 | 格式 |
|------|------|------|
| 工艺内容 | 否 | 每行一条，如：三针五线大哈苏 |

#### 第三区：SKU 与面料

| 字段 | 必填 | 说明 |
|------|------|------|
| SKU颜色 | 否 | 该SKU的颜色 |
| 尺码 | 否 | 如 M/L/XL |
| 件数 | 否 | 默认 1 |

每块面料（布类自动分配 A/B/C…）：

| 字段 | 必填 | 说明 |
|------|------|------|
| 面料编号 | 是 | 如 JQ5668，用于系统面料库匹配 |
| 色号 | 否 | 如 5665-2(CG) |
| 颜色名 | 否 | 如 活性黑 |
| 罗纹 | 否 | 是/否 |
| 幅宽 | 否 | 如 183CM |
| 克重 | 否 | 如 340G |
| 供应商 | 否 | 如 嘉谦纺织 |
| 成分 | 否 | 如 85%棉8.5%聚酯纤维6.5%氨纶 |
| 品名 | 否 | — |
| 米长/米价/采购人/到货 | 否 | 采购人默认同设计师 |

#### 第四区：辅料（可选）

| 字段 | 必填 | 说明 |
|------|------|------|
| 辅料名称 | 是 | — |
| 规格/颜色/用量/单位/单价/供应商/电话/采购量/采购人/到货 | 否 | 采购人默认同设计师 |

> **⚠️ `assets/form.html` 保留不动**：该文件是完整交互表单模板，待 WorkBuddy 修复 `sendPrompt()` 回传问题后恢复使用。届时修改本节 A.1 策略即可。

> 🚫 **铁律 — 原样渲染（恢复后生效）**：
> `assets/form.html` 的内容**必须原封不动、逐字节传**入 `show_widget`。
> **严禁**对表单做任何修改（新增/删除/调整字段/标签/布局/CSS/JS）。唯一正确做法：`Read assets/form.html` → 直接传 `widget_code`。

### A.2 生成Excel — 按模板写入

收集完成后，Agent 调用 `excel_writer.py` 生成统一格式 Excel，保存到 `桌面/{款号}.xlsx`。

### A.3 审核预览

聊天窗口展示简化审核表。设计师必须明确回复"确认"后才能提交。

### A.4 API 提交

登录沙箱 → 上传图片 → 组装 payload → `POST insertDesign`。

> ⚠️ **提交门槛**：A.3 设计师明确说"确认"之前，不得执行 A.4。

---

## 场景 B：审查提交（Excel 已填）

```
Excel + 图片 → Agent审查Excel → 列出缺失/疑问 → 设计师补充 → 确认 → API提交 → 显示结果
```

### B.1 审查 Excel

Agent 读取设计师提供的 Excel 文件，逐区检查：

| 检查区 | 必填项 | 常见缺失 |
|--------|--------|---------|
| 基础信息 | 款号、需求类型、客户、衣服类型、款式、设计师 | 版型、烫唛、完成时间 |
| 面料区 | 布类(A/B/C)、编号、色号、颜色名、成分、克重、幅宽、供应商 | 采购人(cgBy)、螺纹标记 |
| 尺码 | 尺码、件数 | — |
| 工艺区 | 工艺内容 | — |
| 辅料区 | 名称、规格、颜色、用量、单位、采购单价、供应商、联系方式、采购数量、采购人、到货时间 | 采购人 |

### B.2 列缺失清单

以表格形式列出所有缺失/待确认项，让设计师补充。不逐条重问已有数据。

### B.3 补充确认

设计师补充缺失项后，展示最终审核表。设计师明确回复"确认"后提交。

### B.4 API 提交

同 A.4。

---

## 设计师最终文件

- **场景 A**：JPG 图片 + Agent 生成的 Excel（`桌面/{款号}.xlsx`）
- **场景 B**：JPG 图片 + 设计师原 Excel（Agent 不新建）

---

## API 数据结构注意事项（铁律）

从多轮调试中总结的关键规则，提交时必须遵守：

### fabricList 必填字段

| 字段 | 说明 | 示例 |
|------|------|------|
| `productNo` | **面料编号**（必须！否则前端无法匹配） | `"JQ5665"` |
| `clothType` | **布类**：第一块A，第二块B，第三块C... | `"A"` / `"B"` |
| `color` | 色号 | `"5665-2(CG)"` |
| `colorName` | 颜色名 | `"活性黑"` |
| `cgBy` | **采购人**（默认同设计师） | `"设计者姓名"` |
| `fabricId` | 面料库ID | `167` |
| `supNo` / `supName` | 供应商编号 / 名称 | `"G0002"` / `"嘉谦纺织"` |
| `component` | 成分 | `"85%棉8.5%聚酯纤维6.5%氨纶"` |
| `buffon` | 幅宽 | `"183CM"` |
| `grem` | 克重 | `"340G（回后）"` |
| `thread` | 螺纹标记 | `"0"` 否 / `"1"` 是 |
| `sort` | 排序 | `1, 2, 3...` |

### SKU 字段

- `sku` / `skuName`：保持独立标识（如 `"SKU1"`），**禁止填入面料编号**
- SKU 行颜色：需单独询问设计师（不自动从面料取）

### 其他

- `layout`（版型）：必填
- `customerCode`：从客户API模糊匹配获取；无匹配时留空
- `cgBy` 规则：面料/辅料的采购人**默认同使用者姓名**。**但 Excel 表格中采购人列如有明确值（非空、非"请选择"），以表格值为准，不覆盖。**

### 款号为空时的处理

若 Excel 中款号（B6）未填：

1. API 提交时 `itemNum` 留空 —— 系统自动生成款号
2. 提交成功后，查询已创建的设计单，获取系统自动生成的款号
3. 用该款号重命名：
   - Excel 文件：`原文件名` → `{系统款号}.xlsx`
   - 图片文件：单图 `{系统款号}.jpg`；多图 `{系统款号}_A.jpg`, `{系统款号}_B.jpg`
4. 在对话中告知设计师："款号未填，系统已自动生成款号 XXX，文件已同步重命名"

### 多图片命名规范

若文件夹中有多张图片，需按以下规则命名（空格用下划线替代）：

| 文件名 | 无款号时 | 有款号时 | 上传位置 |
|--------|---------|---------|---------|
| 基础信息图 | `A.png` | `{款号}_A.png` | 基础信息（belong=0） |
| SKC详情图 | `B.png` | `{款号}_B.png` | SKC详情（belong=1） |
| 工艺详情图 | `C.png`, `C1.png`, `C2.png`... | `{款号}_C.png`, `{款号}_C1.png`... | 工艺详情（belong=2），可多张 |

示例：
```
文件夹/
├── 293B09W-S_A.png    → 基础信息
├── 293B09W-S_B.png    → SKC详情
├── 293B09W-S_C.png    → 工艺详情(1)
└── 293B09W-S_C1.png   → 工艺详情(2)
```

## Step 0：准备工作

### 0.1 首次使用：收集使用者身份

本 Skill 安装后首次加载时，必须向使用者询问以下信息：

> 欢迎使用工艺单上传 Skill！首次使用需要配置你的信息：
>
> **A. 身份信息**（用于自动填入设计师和采购人）
> 1. 你的真实姓名（如 张三）→ 面料/辅料的采购人(cgBy)默认填你
> 2. 你在打板系统里的设计师名字（如 张三）
> 3. 你在系统里的设计师编号（如 75）
>
> **B. 嘉谦系统登录凭据**（用于 API 自动上传）
> 4. 嘉谦系统的登录账号
> 5. 嘉谦系统的登录密码

收集后写入 `src/.identity.json`：
```
MC_USER_REALNAME=张三
MC_USER_DESIGNER_ID=
MC_USER_DESIGNER_NAME=
MC_USERNAME=
MC_PASSWORD=
```

### 0.2 采购人(cgBy)规则

采购人和指派设计师**默认均为使用者姓名**（`MC_USER_REALNAME`），除非：

| 情况 | 行为 |
|------|------|
| Excel 表格中采购人列为空 | → 填使用者姓名 |
| Excel 表格中采购人列有值（如"圆圆"） | → **以表格中的值为准**，不覆盖 |
| 辅料的采购人列为空 | → 填使用者姓名 |
| 辅料的采购人列有值 | → **以表格中的值为准** |

> 换言之：使用者姓名是默认值，表格里有明确写明的采购人具有更高优先级。

### 0.2 每次使用：加载 Excel + 预加载系统数据

1. 确认 Excel 模板可访问：`成衣设计系统_标准化模板.xlsx` → Sheet `成衣工艺制单`
2. 设计师准备好的图片按 A/B/C1/C2 命名，用于上传
3. 调用 API 预加载以下系统数据（缓存到对话上下文中，整个对话期间复用）：
   - **客户列表** ← `GET /business/customer/public`（重要：用于客户名称模糊匹配）
   - 面料库(902条)、辅料库(55条)
   - 烫唛列表、设计师列表
   - 三类工艺（印花/洗水/车缝）

> ⚠️ 客户列表必须预加载。后续 1.1 客户名称匹配需要用到完整的客户库数据。

## Step 1：对话填表

### 核心原则
- 必填项(*)必须问，非必填项先问"要不要填"
- 面料：先问数量，再逐个问编号 → 在库自动匹配，不在库手动输入
- 布类：自动 A/B/C/D，无需问设计师
- 辅料：先问有没有，有就逐条问

---

### 1.1 必填项（先问，6个）

> 这个工艺单的**款号**是什么？（如 293B09W-S）

> | 项 | 选项 |
> |---|---|
> | 需求类型* | OEM、ODM、OBM |
> | 客户名称* | 输入后自动模糊匹配 → 从匹配结果中选择（见下方详细流程） |
> | 衣服类型* | T、W、J、K、B、F、C、P、H、S、Q |
> | 款式* | 短袖T恤、短裤、长裤、长袖T恤、半拉链外套、连帽开衫外套、圆领卫衣、连帽卫衣、短裙、连衣裙、短袖衬衫、长袖衬衫、拉链开衫外套、背心、polo衫、包包、套头卫衣、开衫卫衣、半拉链卫衣 |
> | 指派设计师* | 系统设计师列表选 |

> 打版件数默认 1，需要增加吗？

#### 客户名称匹配流程（核心改动）

设计师告知客户名称后（如"空喊中心"），**不要直接使用**，必须执行以下匹配流程：

```
设计师: "客户是空喊中心"
       ↓
Agent: 对 Step 0.2 预加载的客户列表做模糊匹配
       匹配算法（见 customer_matcher.py）：
         - 拼音首字母匹配（"空喊中心"→khzx, "哭喊中心"→khzx → 同音匹配）
         - 编辑距离相似度（处理缺字/多字/错字）
         - 组合评分排序
       ↓
  ┌─ 得分 ≥ 0.85（高置信度单匹配）
  │   → "在客户库中找到：哭喊中心 (C0005)，确认吗？"
  │   → 用 AskUserQuestion: [确认] [查看其他匹配]
  │
  ├─ 得分 ≥ 0.4，多个匹配
  │   → "找到 N 个可能匹配，请选择："
  │   → 用 AskUserQuestion 列出所有匹配项，每个显示：客户名称 + 编码
  │   → 最多展示 5 个，超过则加 [查看更多...]
  │
  └─ 无匹配（得分 < 0.4）
      → "未在客户库中找到匹配项。请选择："
      → AskUserQuestion:
          [显示前4~5个全部客户供浏览]
          [手动输入（不计入客户库，customerCode留空）]
```

**关键规则：**
- ✅ 匹配到客户后，**同时记录 customerName 和 customerCode**
- ✅ "手动输入" 只记录 customerName，customerCode 留空
- ✅ 客户列表已缓存在对话上下文，无需再次调用 API
- ❌ 禁止直接用设计师输入的文字作为 customerName（除非手动输入路径）

### 1.2 非必填项（选填）

> 需要填写以下非必填信息吗？不需要的话直接跳过。
> - 版型、预计完成时间、烫唛

| 项 | 说明 |
|------|------|
| 版型 | 自由文本 |
| 预计完成时间 | YYYY-MM-DD |
| 烫唛 | 从19项唛头列表选 |

> 注意：客户名称已在 1.1 中通过模糊匹配确认（含 customerCode）。版次系统自动生成，不询问。

### 1.3 面料与辅料数量

> 这件衣服用几种面料？需要填辅料吗？有几种？

根据回答确定后续交互范围，如"3种面料，1种辅料"。

### 1.4 面料信息

按面料逐一处理。布类自动分配：第一个=A，第二个=B，第三个=C。

**每块面料：**

> 面料 **[A/B/C]** 的编号是什么？（如 JQ5668）

→ 在系统面料库(902条)搜索：
- **在库**：自动填入成分/克重/幅宽/供应商/品名，展示给设计师确认
- **不在库**：逐项询问 → 成分？克重？幅宽？供应商？

> 色号和颜色？

- 在库 → 展示该面料的颜色列表(colorTypeVOS)供选择
- 不在库 → 设计师手动输入

> 是螺纹吗？（是/否）

采购人自动同设计师(cgBy)。**仅当设计师明确说"面料/辅料的采购人是XXX"时才修改。**

### 1.5 尺码与件数

> 需要哪些尺码？各做几件？

| 尺码 | XS | S | M | L | XL | XXL | XXXL |

### 1.6 工艺制作（弹窗多选）

> 需要哪些工艺？（三类各可多选）
>
> **印花**（67项）：哑光胶浆、厚板印、发泡印、水浆、拨印...
> **洗水**（19项）：普洗、酵洗、炒雪花、喷马骝...
> **车缝**（18项）：三针五线大哈苏、两针四线大哈苏、0.2边线...

选中项写入 Excel 工艺区(cate: 0=印花, 1=洗水, 2=车缝)。

### 1.7 辅料清单（如有）

逐条询问 11 个字段（图片由设计师在系统前端上传）：

> 辅料 **第[1/2/3...]** 条：
> | 字段 | 说明 |
> |---|---|
> | 辅料名称* | 从辅料库55项选，或不在此列手动输入 |
> | 规格型号 | |
> | 颜色 | |
> | 用量 | 单件用量 |
> | 单位 | 条/个/米/对... |
> | 采购单价 | 元 |
> | 供应商 | |
> | 联系方式 | 供应商电话 |
> | 采购数量 | 采购总量（区别于单件用量） |
> | 采购人 | 默认同设计师 |
> | 要求到货时间 | YYYY-MM-DD |

---

## Step 2：生成 Excel 存档

Step 1 填表完成后，**先生成 Excel 保存到桌面**，再展示审核。

### 2.1 调用方式

```bash
cd "F:\WORKBUDDY WS\工艺单数据表单自动化" && python src/submit_excel.py
```

### 2.2 输入

填表过程中收集的全部结构化数据（JSON），与 Step 3 提交 API 用的同一份。

### 2.3 输出

| 项目 | 说明 |
|------|------|
| 文件名 | `{款号}.xlsx` |
| 保存路径 | `C:\Users\Administrator\Desktop\{款号}.xlsx` |
| 模板 | `成衣设计系统_标准化模板.xlsx` → Sheet `成衣工艺制单` |

> ⚠️ Excel 模板文件路径：项目根目录 `config/成衣设计系统_标准化模板.xlsx`

### 2.4 写入规则

按照 Excel 模板映射表写入对应单元格（见文档末尾 `Excel 模板映射` 章节）。

---

## Step 3：审核预览

Excel 已保存到桌面后，**在聊天窗口展示简化审核表**，等待设计师确认。

### 展示格式

```
┌────────────────────────────────────────┐
│  📋 工艺单审核预览                      │
├────────────────────────────────────────┤
│  款号: CONP-2026-001                    │
│  衣服类型: T (T恤)                      │
│  需求类型: ODM   客户: 鸿利制衣(C0006)  │
│  设计师: 游雅倩     版次: 头版          │
│  烫唛: 漫威龙烫唛                       │
│                                         │
│  面料A: JQ5668 10#黑 135g 150cm         │
│  面料B: JQ5635 17#深灰 135g 150cm       │
│  面料C: 1877 2#黑 160g 173cm            │
│  尺码: M×1, L×2, XL×1                   │
│                                         │
│  工艺: 三针五线 / 洗水酵洗              │
│  辅料: 拉链×1条                         │
│                                         │
│  💾 Excel已保存: 桌面/CONP-2026-001.xlsx│
└────────────────────────────────────────┘
回复 "确认" 提交, 或 "修改XX" 调整
```

### 确认门槛（CRITICAL）

> ⚠️ **设计师必须明确回复"确认"后，才能执行 Step 4 提交。**
> 未收到"确认"之前，只允许修改数据并重新生成 Excel / 重新展示审核表。

## Step 4：自动提交

设计师确认后，Agent 在后台执行：

```bash
cd "F:\WORKBUDDY WS\工艺单数据表单自动化" && python src/main.py "<图片路径>"
```

脚本自动完成：登录 → 查询辅助数据 → 上传图片 → 组装 payload → `POST insertDesign`

## Step 5：显示结果

```
✅ 提交成功！
  款号：CONP-2026-001
  设计单号：2026062404
  提交时间：2026-06-24 16:44
```

---

## 沙箱环境

| 项目 | 值 |
|------|-----|
| 地址 | https://mctwo.fsjqfz.xyz |
| API基路径 | /makeCloth |
| 用户名 | （首次运行自动收集） |
| 密码 | （首次运行自动收集） |
| 登录接口 | `POST /makeCloth/colorimeter/index/login` |
| 图片上传 | `POST /makeCloth/business/upload/img` |
| 创建设计单 | `POST /makeCloth/business/design/insertDesign` |
| 编辑设计单 | `POST /makeCloth/business/design/updateDesign` |
| 设计单查询 | `POST /makeCloth/business/design/selectList` |
| 设计师查询 | `GET /makeCloth/dept/getUserList/name?deptName=设计部` |
| 客户查询 | `GET /makeCloth/business/customer/public` |
| 唛头查询 | `POST /makeCloth/business/markLabel/select` |

## Excel 模板映射

模板文件：`成衣设计系统_标准化模板.xlsx` → Sheet `成衣工艺制单`

| Excel 单元格 | 对应API字段 | 说明 |
|-------------|-----------|------|
| F5 | itemNum | 款号 |
| B6 | needType | 需求类型(OEM/ODM/OBM) |
| D6 | customerName | 客户名称 |
| F6 | customerCode | 客户编码 |
| B7 | clothType | 衣服类型(T/W/J/K...) |
| D7 | style | 款式 |
| B8 | num | 打版件数(公式自动) |
| D8 | version | 版次 |
| F8 | designName | 指派设计师 |
| B9 | finishTime | 预计完成时间 |
| B10 | markName | 烫唛 |
| H8 | sewing | 车缝工艺 |
| SKC面料区(行30+) | designColors[].fabricList | 面料/A/B/C/D |
| C14-C27 | works[] | 工艺制作+后道 |
| R233+ | auxiliaries[] | 辅料清单 |
